#!/bin/bash

while read -r -d $'\0' kv; do
    if [[ "${kv:0:5}" == "TERM=" ]]; then
        echo "export $kv"
    elif [[ "${kv:0:8}" == "DISPLAY=" ]]; then
        echo "export $kv"
    fi
done < /proc/1/environ
