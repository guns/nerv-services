#!/bin/bash

while read -r -d $'\0' kv; do
    if [[ "${kv:0:5}" == "TERM=" ]]; then
        echo "$kv"
    elif [[ "${kv:0:8}" == "DISPLAY=" ]]; then
        echo "$kv"
    fi
done < /proc/1/environ
