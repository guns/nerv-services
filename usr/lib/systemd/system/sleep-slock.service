[Unit]
Description=Screen lock on sleep
Before=sleep.target
Requires=restrict-sysrq.service

[Service]
Type=simple
EnvironmentFile=/etc/conf.d/slock
ExecStart=/bin/sh -c 'exec sudo -u "${USER}" /usr/bin/env DISPLAY="${DISPLAY}" /usr/bin/slock'

[Install]
WantedBy=sleep.target
